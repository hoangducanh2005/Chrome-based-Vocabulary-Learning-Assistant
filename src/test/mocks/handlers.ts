import { http, HttpResponse } from 'msw';

// Add mock request handlers here
export const handlers = [
  // Example:
  // http.post('https://api.example.com/translate', () => {
  //   return HttpResponse.json({ translation: '...' });
  // }),
];
